<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2019 Akretion
    License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="sale_order_view_form" model="ir.ui.view">
        <field name="name">sale.order.form.sale.workflow</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_deliver"
                    type="object"
                    string="Deliver"
                    class="oe_highlight"
                    states="sale"
                    data-hotkey="v"
                    groups="sales_team.group_sale_salesman"
                />
                <button
                    name="action_partial_deliver"
                    type="object"
                    string="Partial Deliver"
                    states="sale"
                    groups="sales_team.group_sale_salesman"
                />
            </xpath>
            <field name="state" position="after">
                <field name="delivery_state" widget="statusbar" statusbar_visible="pending,partial,delivered" />
            </field>
        </field>
    </record>

    <record id="view_order_tree" model="ir.ui.view">
        <field name="name">sale.order.tree.sale.workflow</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="delivery_state" />
            </field>
        </field>
    </record>

    <record id="view_quotation_tree" model="ir.ui.view">
        <field name="name">sale.quotation.tree.sale.workflow</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree" />
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="delivery_state" />
            </field>
        </field>
    </record>

    <record id="view_sales_order_filter" model="ir.ui.view">
        <field name="name">sale.order.search.sale.workflow</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="delivery_state" />
            </field>
            <filter name="order_month" position="after">
                <filter
                    name="delivery_state"
                    string="Delivery State"
                    domain="[]"
                    context="{'group_by':'delivery_state'}"
                />
            </filter>
        </field>
    </record>
</odoo>
